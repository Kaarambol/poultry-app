{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "node scripts/db-deploy.js && next build",
  "crons": [
    {
      "path": "/api/cron/weekly-backup",
      "schedule": "0 3 * * 1"
    }
  ]
}